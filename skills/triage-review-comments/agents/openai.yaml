interface:
  display_name: "Triage Review Comments"
  short_description: "Classify PR feedback and scope approved fixes"
  default_prompt: "Use $triage-review-comments to inventory and classify current PR feedback; keep triage read-only unless I explicitly ask you to fix, resolve, or track items."
