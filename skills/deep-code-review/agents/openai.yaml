interface:
  display_name: "Deep Code Review"
  short_description: "Audit repository changes beyond the diff"
  default_prompt: "Use $deep-code-review as the repository-wide umbrella to review this change safely, validate only change-caused defects, and assess correctness, security, compatibility, and merge readiness."
