interface:
  display_name: "Clean Merged Git Branch"
  short_description: "Clean merged Git branches safely"
  default_prompt: "Use $git-clean-merged-branch to clean up this merged local branch while preserving its remote branch unless I explicitly request remote deletion."
