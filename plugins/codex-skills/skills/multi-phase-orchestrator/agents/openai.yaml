interface:
  display_name: "Multi-Phase Orchestrator (Beta)"
  short_description: "Beta worktree-thread orchestration"
  default_prompt: "Use $multi-phase-orchestrator to coordinate these work units with fresh worktree threads, required skills per unit, coordinator-owned validation, committed integration, and cleanup."

policy:
  allow_implicit_invocation: false
